const currentHost = window.location.hostname
const currentPort = window.location.port
const baseUrl = `${currentHost}${currentPort ? ':' + currentPort : ''}`;
export let HOST_API_SERVER = 'https://mes-admin.serveousercontent.com'
export let HOST_API_SERVER_AUTH = 'https://mes-admin.serveousercontent.com'
export let HOST_SOCKET = 'https://mes-admin.serveousercontent.com'